# Rubrics — CSV Cleaner CLI

## 评分总览

总分 100 分，按清洗规则难度和重要性分配权重。

## 评分细则

### 1. BOM 移除（10 分）

| 分数 | 标准 |
|------|------|
| 10 | 正确检测并移除 UTF-8 BOM（`EF BB BF`），输出文件不包含 BOM |
| 5 | 尝试处理 BOM 但方式不对（如在文本层处理而非字节层，导致首字段含 `﻿`） |
| 0 | 完全未处理 BOM |

### 2. 空格修剪（15 分）

| 分数 | 标准 |
|------|------|
| 15 | 所有字段（含表头）的首尾空格均被正确修剪 |
| 10 | 修剪了数据字段但遗漏了表头 |
| 5 | 部分修剪（如只去除了左空格或只去除了右空格） |
| 0 | 未做任何空格修剪 |

### 3. 空行删除（15 分）

| 分数 | 标准 |
|------|------|
| 15 | 正确删除所有空行（包括全空白行和仅含空格/制表符的行），表头不受影响 |
| 10 | 删除了完全空行但遗漏了仅含空格的行 |
| 5 | 删除逻辑有误（如误删了表头行或有数据的行） |
| 0 | 未删除空行 |

### 4. 列数对齐（20 分）

| 分数 | 标准 |
|------|------|
| 20 | 正确补齐不足列（填充空字符串）、截断多余列，且不改变行顺序 |
| 10 | 只实现了补齐或只实现了截断 |
| 0 | 未处理列数不一致的情况 |

### 5. 重复行删除（15 分）

| 分数 | 标准 |
|------|------|
| 15 | 正确删除所有重复数据行，保留首次出现，表头不参与去重 |
| 10 | 去重逻辑基本正确但包含了表头行 |
| 5 | 只去除了连续重复行而非全局去重 |
| 0 | 未做任何去重 |

### 6. CSV 正确解析（25 分）

这是权重最高的维度，因为它直接影响其他所有规则的正确性。

| 分数 | 标准 |
|------|------|
| 25 | 使用 `csv` 模块正确解析，能处理：引号内逗号、引号内换行符、转义引号（`""`） |
| 15 | 能处理引号内逗号但换行符或转义引号处理有误 |
| 10 | 使用了手动字符串分割（如 `line.split(',')`），不能处理引号内逗号 |
| 0 | 完全无法解析含特殊字符的 CSV |

---

## 自动评测 vs 人工评测

### 由脚本自动评测的维度

| 维度 | 评测方式 | 对应规则 |
|------|----------|----------|
| BOM 移除 | `test_logic.py` 逐格对比首字段值（`id` vs `﻿id`） | 规则 1 |
| 空格修剪 | 逐格对比每个字段值（` Alice ` vs `Alice`） | 规则 2 |
| 空行删除 | 行数校验（期望 11 行 vs 实际行数） | 规则 3 |
| 列数补齐 | 检查每行列数是否一致，补齐字段是否为空串 | 规则 4 |
| 列数截断 | 检查多余列是否被丢弃 | 规则 4 |
| 重复行删除 | 逐格对比时发现重复行会导致行数不匹配 | 规则 5 |
| CSV 解析（引号内逗号） | 对比含逗号的字段是否完整保留 | 规则 6 |
| CSV 解析（多行字段） | 对比含 `\n` 的字段是否完整保留 | 规则 6 |
| CSV 解析（转义引号） | 对比含 `""` 的字段是否正确还原 | 规则 6 |
| UTF-8 输出编码 | `test_logic.py` 以 UTF-8 打开文件，非 UTF-8 直接报错 | — |
| 输出文件存在性 | `test.sh` 用 `[ ! -f "$ACTUAL_OUTPUT" ]` 检查 | — |
| 脚本正确退出 | `test.sh` 检查 exit code（0 = 通过） | — |

**自动化覆盖：12 个维度，涵盖全部 6 条清洗规则。**

### 需要人工评测的维度

| 维度 | 说明 |
|------|------|
| 代码可读性 | 变量命名、注释质量、代码结构是否清晰 |
| 是否使用 stdlib | 是否遵守了"仅限标准库"约束（使用 pandas 应扣分） |
| 错误处理 | 输入文件不存在时是否有合理报错而非 crash |
| 算法效率 | 对于大文件的内存使用是否合理（流式 vs 全量加载） |

---

## 得分等级

| 等级 | 分数区间 | 说明 |
|------|----------|------|
| A（优秀） | 90-100 | 全部 6 条规则完美实现，代码质量高 |
| B（良好） | 75-89 | 核心规则正确，个别边界情况有遗漏 |
| C（及格） | 60-74 | 基本功能可用，但 CSV 解析不够健壮 |
| D（不及格） | 0-59 | 多个规则未实现或实现有严重缺陷 |
